# Shared dependencies for both POC projects
# Install with: pip install -r requirements.txt

# Core Hugging Face libraries
transformers>=4.38.0
tokenizers>=0.15.0

# Deep learning backend (CPU build; replace with torch+cu121 for GPU)
#torch>=2.2.0 - very big for our ram!
torchaudio>=2.2.0

# Audio I/O – required for the speech-to-text project
soundfile>=0.12.1
librosa>=0.10.1

# Utilities
accelerate>=0.27.0      # speeds up model loading on multi-core CPUs / GPU
tqdm>=4.66.0            # progress bars
